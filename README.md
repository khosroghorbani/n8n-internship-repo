1) توضیح کوتاه:
این کامپوننت یک دکمهٔ پرداخت ساده و آمادهٔ استفاده است که متن قابل پیکربندی (مثلاً نمایش مبلغ) دارد و هنگام کلیک حالت بارگذاری (loading) را نشان می‌دهد.

2) کامپوننت‌های DIG استفاده‌شده و دلیل انتخاب:
- registry/dig/components/ui/button (Button): دکمهٔ استاندارد واریانت‌پذیر دیگ که همهٔ حالات لازم (loading، اندازه، رنگ و ... ) را پشتیبانی می‌کند. انتخاب شد تا از استایل‌ها و رفتارِ ازپیش‌تعریف‌شدهٔ دیگ استفاده شود و کد ساده و قابل نگهداری بماند.

3) آیکون‌های DIGCHEH استفاده‌شده و دلیل انتخاب:
- هیچ آیکونی استفاده نشده است (درخواست، دکمهٔ پرداختِ ساده بود؛ طبق قانون‌ها در صورت درخواست ساده آیکون اضافه نکردیم).

4) مسیر فایل UI:
- src/components/PaymentButton.jsx
